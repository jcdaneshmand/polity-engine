# Private bot table workflow
Bot table CSV import is scaffolding-only right now. The template CSVs in `private-card-data/` show the intended private local row shape, but the bot table import and validation scripts intentionally fail until the importers are implemented.

Keep any private CSVs and generated JSON local and gitignored. Do not treat the bot table workflow as runnable until the import scripts produce normalized tables and reports.
