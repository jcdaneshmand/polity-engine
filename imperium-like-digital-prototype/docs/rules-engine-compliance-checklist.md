# Rules Engine Compliance Checklist

Use this checklist before opening any PR that changes engine logic, data, or rules docs.

Primary reference: `docs/rules-engine-notes.md`.
External reference: the public Imperium: Horizons rulebook PDF. Do not copy official prose or card text into this repo.

## Required checks

- [ ] **Legal boundary:** No official card text, art, logos, faction names, or rulebook wording added.
- [ ] **Original data only:** Any new cards/civs/resources are original placeholder content.
- [x] **Card IDs in zones:** Runtime zones store string IDs, not full card objects.
- [x] **Visibility:** Hidden zones expose counts only; Development area and public piles expose face-up card IDs.
- [x] **Determinism:** New randomness is routed through boardgame.io random APIs when in move resolution.
- [x] **Move signatures:** boardgame.io moves use the current callback shape (`{ G, ctx, events, random }`, then args).
- [x] **Turn lifecycle:** End-turn behavior advances framework turn state (not just local state mutation).
- [x] **Draw safety:** Draw loops terminate when no cards are drawable.
- [x] **Draw-if-able safety:** Draw-if-able effects never trigger reshuffle.
- [x] **Nation progression:** Reshuffle adds the current top card from the ordered Nation deck before shuffling discard.
- [x] **Development progression:** After Nation deck exhaustion, reshuffle offers a payable face-up Development card choice and places the chosen card in discard.
- [x] **State/accession timing:** State flip/token movement happens in the reshuffle step required by the rules contract.
- [x] **Market gain semantics:** Acquire and Break through remain separate operations with correct Unrest/resource/refill side effects.
- [x] **Payment semantics:** Payment, removal, stealing, and resource gain are modeled as distinct operations.
- [x] **Effect runner constraints:** Costs/effects handling remains explicit and data-driven; unsupported ops fail clearly.
- [x] **Solstice/endgame:** Changes that can affect round boundaries, scoring, collapse, or Fame deck timing update tests and notes.
- [x] **State-model consistency:** Changes stay aligned with vocabulary and milestone direction in `rules-engine-notes.md`.
- [x] **High-risk areas reviewed:** Consider reshuffle timing, market refill source logic, garrison movement, payment substitution, hidden info, and endgame interrupt implications.

## PR notes requirement

In PR descriptions for engine changes, include a brief "Rules Notes Alignment" section describing which checklist items were touched and how they were satisfied.

## Current Evidence

- Nation progression is covered by `engine/src/tests/progression.test.ts`, `engine/src/tests/setupPipeline.test.ts`, `engine/src/tests/variants.test.ts`, and `engine/src/tests/privateNationImport.test.ts` cases for shuffling regular Nation deck cards at setup while keeping Accession separate, folding `no_accession` setup Accession cards into the hidden Nation deck, drawing opening hands before short-game setup advances or quest-style Nation-card additions, short-game setup advancement consuming separated Accession as the bottom/next Nation card when regular Nation cards run out, short-game Accession setup queueing player-selected Development exile choices unless a nation exception skips them, parking queued short-game Development removals behind intervening pending choices, Arthurian/Polynesian-style short-game overrides skipping Accession Development removal, private/in-memory nation setup defaulting missing compatibility arrays plus missing ruleset tag, setup/zone/state/hook, and short-game arrays to empty, adding the current top Nation card only when an Action token is available, `no_nation_deck` ruleset tags suppressing default Nation-card progression, `no_accession` ruleset tags treating accession-typed Nation cards as regular progression without state flipping, progressing when discard starts empty, placing the reshuffle marker on the Nation deck while Nation progression remains and into Development when the added card empties the Nation deck, preserving Nation-before-Development priority, adding separately tracked Accession before Development, and terminal Nation card handling.
- Development progression is covered by `engine/src/tests/progression.test.ts` cases for payable face-up Development choices, optional skip, payment substitution, unpayable/no-development-area skips, card-effect Develop not spending progression tokens, free card-effect Develop resolving without Development payment, and scoring when the Development area empties, plus `engine/src/tests/setupPipeline.test.ts` setup coverage that `no_development_area` rulesets suppress the initial Development area, and `engine/src/tests/turnLoop.test.ts` move-legality coverage that free Develop text can make a card playable even when no Development card is payable.
- State/accession timing is covered by `engine/src/tests/progression.test.ts` cases for accession flips, single two-sided State cards, active State stat refresh, never-Empire exceptions, in-play/terminal accession-style exceptions, and short-game accession Development removal timing, plus `engine/src/tests/setupPipeline.test.ts` coverage that `starts_as_empire` setup tags activate the empire/civilized State side and explicit setup Action-token overrides survive alternate starting-State activation, and `engine/src/tests/expansionToggle.test.ts` coverage that Trade Routes' extra Exhaust token remains an expansion modifier when state-card stats refresh. Multi-symbol card play legality is covered by `engine/src/tests/turnLoop.test.ts` with a card that can be played when any one listed state matches the active State card.
- Draw safety is covered by `engine/src/tests/progression.test.ts` cases for no drawable cards, unpayable Development, before/after reshuffle interruptions, pending reshuffle draws waiting behind unfinished nation-hook continuations, and failed reshuffle hooks without runaway draw loops, plus `engine/src/tests/turnLoop.test.ts` move-legality coverage for draw text that can resolve through Action-token Nation progression while rejecting `no_nation_deck` Nation-card progression as a draw source and zero-count draw-only text, and `engine/src/tests/effectRunner.test.ts` optional-effect coverage for the same `no_nation_deck` draw-legality gate.
- Draw-if-able safety is covered by `engine/src/tests/effectRunner.test.ts`; `draw_if_able` draws only from the current draw deck and leaves discard/Nation progression untouched when the deck is empty, including voluntary `draw_if_able.upTo` choices that cap options at the currently drawable deck count. Move-level legality in `engine/src/tests/turnLoop.test.ts` also rejects zero-count draw-if-able-only text, rejects discard reshuffle as draw-if-able legality, and covers multi-count draw-if-able text drawing only the available deck cards without reshuffling discard or advancing Nation/Development progression.
- Card-ID zone storage is enforced by `engine/src/game/state.ts` zone and pending-choice types, by setup/import tests that assert zone arrays contain IDs, and by movement tests that compare zone contents by card ID rather than embedded card objects.
- Visibility is covered by `engine/src/tests/uiSelectors.test.ts`, `engine/src/tests/uiSelectionModel.test.ts`, and `engine/src/tests/privateCardEntryNavigation.test.tsx` cases for hidden draw/Nation/Bot decks, separately tracked or embedded-bottom Accession exposing the public bottom Nation deck card while preserving hidden regular Nation-card counts, count-only opponent hands, History, History replacement zones, setup-time History placements routed through replacement zones, setup-created and History-replacement side-area zone chrome counts/data labels plus selected detail labels, non-public setup-created side-area privacy, public Development/Exile zones, public small-deck bottom cards, face-up Fame special-bottom timing, and per-player looked-card access. `engine/src/tests/setupPipeline.test.ts` also covers setup-created side-area display/visibility metadata, replacement History zone display/visibility/scoring metadata, and disabled-History alternate-zone metadata being recorded on game state. The same separately tracked Accession-as-bottom-Nation-card semantic is covered for ruleset zone conditions by `engine/src/tests/progression.test.ts` and collapse/auto-win zone checks by `engine/src/tests/scoring.test.ts`.
- Determinism is covered by `engine/src/tests/setupPipeline.test.ts`, `engine/src/tests/progression.test.ts`, `engine/src/tests/soloBotReview.test.ts`, and move-loop tests that route setup shuffles, reshuffles, Bot cleanup, Bot Trade Route effects reached through Bot turns, Bot setup, discard-random, direct and spend-triggered after-gain-Unrest hooks, Find shuffles, Break through deck search, and Solstice/cleanup continuations through seeded or injected random functions. Setup option snapshots are covered for top-level and nested campaign-progress caller mutations. Bot state-table metadata matching is covered for suit, normalized card-type triggers, normalized Unrest triggers, and imported `suit:unrest` icon metadata.
- Move signatures and boardgame.io integration are covered by `engine/src/game/game.ts`, which wires moves and turn hooks with the callback-object shape, and by `engine/src/tests/turnLoop.test.ts` cases that call moves with `{ G, ctx, events, random }`, including `events.endTurn` handoff assertions.
- UI action availability is covered by `engine/src/tests/uiSelectionModel.test.ts` cases that keep normal Play and Profit disabled when the player has no available Action token, while allowing explicit Free play behavior to remain tokenless and exposing pending card-effect-created Free Play choices through `resolveFreePlayChoice`. `engine/src/tests/turnLoop.test.ts` also covers Free Play waiving only the Action-token cost while still requiring printed resource costs before the card can resolve.
- Turn lifecycle is covered by `engine/src/tests/turnLoop.test.ts`, `engine/src/tests/scoring.test.ts`, `engine/src/tests/soloPracticeModes.test.ts`, `engine/src/tests/progression.test.ts`, `engine/src/tests/effectRunner.test.ts`, and `engine/src/tests/tradeRoutesModule.test.ts` cases for `endTurnMove` invoking the framework turn event only after cleanup/pending choices resolve, normal actions being blocked by pending choices and internal lifecycle continuations including play/acquire resolution, Market-Unrest hook, nation-hook, Unrest, Development, reshuffle, scoring/collapse, Solstice, Practice cleanup continuations, and parked counted Region continuations, cleanup Market-resource placement auto-resolving when only one Market destination exists, keeping structured Market slot resource markers in sync, and pausing only for real multi-card choices, cleanup resetting gained Actions above the State card base back to the State card's normal Action count, cleanup draw-up pausing and resuming through reshuffle choices and after-reshuffle reactive Exhaust windows while parent cleanup cursors do not block owned reshuffle continuations, cleanup custom effects pausing and resuming around pending choices and reactive Exhaust windows, cleanup preserving cards that remain in play, ordinary resolved action cards moving to discard while resolved in-play cards stay in play, Precious Cards and nation cleanup overrides suppressing only the voluntary cleanup discard choice, Solstice boundary handoff, Solstice default State flipping when imported rulesets omit state overrides, Solstice pause guards behind reshuffle lifecycle continuations, Solstice suppressing reactive Exhaust windows, Solstice owner-order choices before Return-Exhaust-token interruptions, Revolt and Innovate suppressing reactive Exhaust windows during special-turn resource gains and passive hooks, deterministic priority ordering and continuation for data-driven passive hooks including hook-created discard choices, scoring/collapse interruption, cleanup continuation, Practice market-churn timing including structured Market slot resource marker mirroring and the optional tokenless-Market Exile window excluding both Market-resource and generic card-state token markers, Revolt returning selected Unrest while firing deterministic and choice-producing `after_revolt` passive hooks before cleanup, Innovate Break-through passive choices resuming into cleanup, Action keyword gain/spend effects including normal play legality for Action-token-only text and Market Exile legality rejecting tokened Market cards before the action is spent, normal Play/Acquire/Profit and ordinary Exhaust actions being blocked outside Activate turns, Action marker placement before action text resolves and persistence after played and Profit actions leave play or pause before cleanup, Exhaust marker placement before ability resolution, Return-Exhaust-token effects including same-ability token return, multi-card choice/resume, and later costs staying behind the pending choice, reactive card-play, explicit resource-gain with later resource costs staying behind the reactive window, source-suited in-play resource-gain, card-hosted resource collection, immediate and chosen Trade fallback Progress rewards plus opponent Trade Route Progress rewards pausing before later text/Commerce, Take-Unrest reactive Exhaust windows after both immediate resolution and after-gain-Unrest hook pauses including queued multi-Unrest continuations before the next Unrest is taken, Acquire, and Break-through Exhaust choice/skip timing between effect sentences and during reshuffle continuation, and Exhaust source legality allowing in-play/Power cards while rejecting State cards.
- Market gain semantics are covered by `engine/src/tests/effectRunner.test.ts`, `engine/src/tests/turnLoop.test.ts`, `engine/src/tests/commonsDeckConstruction.test.ts`, `engine/src/tests/setupPipeline.test.ts`, and `engine/src/tests/tradeRoutesModule.test.ts` cases for Market-Unrest tucks mirroring into both legacy Market-Unrest maps and structured Market slot attachments, Acquire/Gain taking tucked Unrest from either representation, Market acquisition tucked-Unrest firing after-gain-Unrest Nation hooks and then resuming on-acquire, after-acquire, Market-resource reactive windows, and later effect text, failed tucked-Unrest hooks rolling back direct Market acquisition state, failed tucked-Unrest hooks during card-effect Market acquisition rolling back the enclosing card play state including resumed multi-Unrest hook continuations, multi-card tucked-Unrest acquisition draining all after-gain-Unrest hooks before follow-up acquisition text, card-effect Market Gain tucked-Unrest firing after-gain-Unrest Nation hooks and then resuming Market-resource reactive windows, after-Take-Unrest reactive windows, explicit Market-choice continuations including Market-resource reactive windows before later resume effects, and later effect text without firing Acquire-only text, multi-card tucked-Unrest Gain draining all after-gain-Unrest hooks before later text while still suppressing Acquire-only text, Take/Break through returning tucked Unrest from either legacy Market-Unrest maps or structured Market slot attachments, market-resource collection from either legacy Market-resource maps or structured Market slot markers including rulebook resource aliases and resource-gain reactive windows before later effect text resumes, source-suited resource-gain reactive windows matching the Market card that released the token, Swap preserving market resources from either legacy Market-resource maps or structured Market slot markers and returning the gained Market card to explicit discard/deck source zones, market refill, main/small deck fallback, Region and Unrest refill ineligibility for tucked Unrest including Region setup-banner replacements and imported `suit:region`/`suit:unrest` icon metadata, Quick Setup still creating player-count-sized Small decks from a combined Market deck while preserving visible Tributary bottom-card metadata including imported `suit:tributary` icon metadata, full setup preserving visible Tributary bottom-card metadata for Break-through choices, generic Acquire creating a player choice when multiple Market cards are eligible, card-effect Acquire and Break-through firing data-driven nation passive hooks, Break-through reactive windows opening once for each gained card before `after_break_through` nation hooks and later printed effects, hook-created choices resuming remaining multi-card Break-through hooks, Innovate Break through suit legality allowing Tributary while excluding Fame, Innovate Break through not firing Acquire-only passives while firing Break-through passives for gained cards, visible Tributary bottom-card Break through choices, pending market choices, Break through not firing acquire-only text, failed Break-through fallback Materials opening resource-gain reactive windows before later effect text resumes, Bot Acquire-from-Exile using public Exile while leaving human-owned Exile unavailable to the Bot and treating imported `suit:unrest` Exile cards as Unrest without taking an extra Unrest card, human Trade fallback preserving Goods when finite Progress supply makes conversion impossible and opening Progress-gain reactive windows before later text whether resolved immediately or by explicit choice, Trade-only play legality rejecting an impossible finite-supply Goods fallback before resolution starts, opponent Trade Route triggers requiring finite Goods and Progress supply before placing the route token and resolving Commerce, opponent Trade Route Progress rewards opening reactive windows before Commerce, Trade choice eligibility including imported `suit:trade_route` icon metadata, Profit legality requiring a completed in-play Trade Route source including imported `suit:trade_route` icon metadata, Profit Goods collection opening resource-gain reactive windows before nested Profit effects resolve, and Profit History destinations routing through both no-History discard behavior and Nation replacement History zones. `engine/src/tests/turnLoop.test.ts` also covers tag-only imported no-History discard routing for pending Find-to-History choices.
- Human Take/Return Unrest handling is covered by `engine/src/tests/effectRunner.test.ts`, `engine/src/tests/exile.test.ts`, `engine/src/tests/scoring.test.ts`, `engine/src/tests/turnLoop.test.ts`, `engine/src/tests/commonsDeckConstruction.test.ts`, and `engine/src/tests/soloBotReview.test.ts` cases for direct Take Unrest, multi-player/short-pile allocation through the move API, targeted Take-Unrest attack effects ignored by protected players, targeted Steal-resource attack effects ignored by protected players, non-card nation/ruleset effects remaining undefendable, Commons setup classifying tag-only imported Unrest into the shared Unrest pile, after-gain-Unrest hooks and their continuation into after-Take-Unrest reactive Exhaust timing, failed after-gain-Unrest hooks during Take Unrest rolling back the enclosing card play state including resumed multi-Unrest continuations, failed after-gain-Unrest hooks during short-pile allocation restoring the pending allocation choice including resumed allocation continuations, queued Unrest-take continuations staying parked behind cleanup discard choices, Solstice order choices, and paused Solstice continuations before taking the next Unrest, short-pile allocation proceeding to immediate Collapse without opening a reactive Exhaust window, paid Progress penalties, human-facing Bot table rows and card text including counted Recall/Abandon Region choices, counted Bot-driven Region choices that resume the Bot turn only after the requested count is satisfied, counted pending Region choices that release resources pausing for reactive Exhaust windows before the next Region selection, Exile acquisition adding the required Unrest to hand rather than discard, Exile acquisition treating tag-only imported Unrest and imported `suit:unrest` icon metadata as Unrest without requiring an extra Unrest card, Exile acquisition required-Unrest opening reactive Exhaust windows before later effect text or pending-choice resume effects, Exile acquisition required-Unrest pausing for after-gain-Unrest Nation choices and then resuming on-acquire, after-acquire, and later effect text, failed required-Unrest hooks during Exile acquisition rolling back the enclosing card play/acquisition state, play legality rejecting Exile-acquire-only action text when required Unrest is unavailable, Return Unrest defaulting to hand only in both effect resolution and play legality, explicit Return Unrest sources such as discard, deck, player/public Exile, History replacement zones, garrisoned play-area Unrest, imported `suit:unrest` icon metadata for Return Unrest move legality and resolution, Return Unrest ignoring printed effects on the returned Unrest card, and normal-scoring Return Unrest choices including garrisoned Unrest attached to scored-zone hosts.
- Garrison child movement is covered by `engine/src/tests/effectRunner.test.ts`, `engine/src/tests/regions.test.ts`, `engine/src/tests/zones.test.ts`, and `engine/src/tests/turnLoop.test.ts` cases for bare Garrison using its source Region as the host, cards tagged `cannot_be_garrisoned` or `not_garrisonable` being excluded from Garrison choices and rejected by direct `garrison_card` resolution, imported `suit:region` icon metadata qualifying a card for Region Recall, targeted garrison Exile, targeted Find from garrison, exact and pending Find movement of play-area hosts, targeted History movement, Power-area self-History movement, disabled-History self-History movement still collecting hosted resources and opening reactive resource windows before later text resumes, targeted Recall/Abandon, host movement carrying children, resource collection from moved cards including rulebook resource aliases and resource-gain reactive windows before later effect text resumes, source-suited resource-gain reactive windows matching the garrisoned card that released the token, and leaving the host in play when only the child is targeted. Find/Nation deck hidden-info coverage in `engine/src/tests/effectRunner.test.ts` also verifies that Accession is excluded whether stored in the physical Nation deck or separately as `accessionCardId`, that `no_accession` rulesets treat accession-typed Nation-deck cards as regular searchable/lookable hidden cards, and that exact-card and criteria Find move the found/selected hidden-deck card before shuffling all searched hidden decks.
- Payment semantics are covered by `engine/src/tests/effectRunner.test.ts` cases for substitution, direct Goods/Progress costs, explicit multi-resource selected payments, selected discard-card costs including later resource costs staying behind pending discard choices, runtime rulebook resource aliases including conditional/resource-movement effects, canonical effect text reading rulebook-named player resource pools, payment from rulebook-named player resource pools, rejecting card-stored resources as ordinary payment sources, after-gain-resource reactive Exhaust matching including later resource costs staying behind the opened reactive window while ordinary finite-supply cost-before-benefit ordering remains intact, steal-resource reactive Exhaust matching keeping later resource costs behind the opened window, and resource-token collection from market cards, default unlimited resources when no shared supply is configured, capped gain/placement and spend-return behavior when finite resource supply is configured, automatic effect-runner cost-before-benefit ordering under finite resource supply, preserving later costs behind unresolved player-choice interruptions, removals, stealing, and returns, by `engine/src/tests/progression.test.ts` Development payment-choice cases including selected-payment overpay rejection, by `engine/src/tests/setupPipeline.test.ts` setup and short-game override resource-alias cases, by `engine/src/tests/turnLoop.test.ts` paid action/exhaust selected-payment validation, overpay rejection for paid actions, Exhaust, and normal Market acquisition, selected discard-card choices keeping later resource costs pending, selected Progress/Goods substitution for normal Market acquisition, automatic choice-cost-before-benefit ordering under finite resource supply, move-level play legality that does not require post-choice costs before the choice can resolve, rulebook-named Solstice empty-resource checks, and returning/removing/stealing Progress without triggering Progress-spent penalties, and by `engine/src/tests/soloBotReview.test.ts` runtime Bot resource-effect alias and Bot market-token collection from both legacy Market-resource maps and structured Market slot markers.
- Effect runner constraints are covered by `engine/src/tests/effectRunner.test.ts` cases for unsupported ops, conditional branch failure propagation, explicit-cost and non-resolvable-text filtering for optional/choose-one effects including payable cost-only branches, optional draw legality when Action-token reshuffle progression can add a Nation card, per-target optional Draw choices, voluntary `draw.upTo` and `draw_if_able.upTo` choices, finite-supply resource-gain legality, finite-supply Break-through fallback legality, zero-token resource-movement filtering inside choice branches, dynamic `targetPlayerScope` for all-player/other-player resource gain, Draw, Steal-resource with per-target fallback, opponent-owned Recall/Abandon Region choices, Take-Unrest effects, targeted Take-Unrest and Steal-resource attack protection, card-effect-created `free_play_card` choices, non-card attack effects remaining undefendable, and Garrison exclusion tags, pending choice interruption/resume including new effects waiting behind pending counted Region-choice, Acquire-effect, Market-move, Break-through post-gain continuations, Free Play choices, and pending scoring finalization, market-only Gain/Take-card source legality, direct and choice-based Market Exile rejecting cards with market resource tokens from either legacy or structured Market slot state and generic card-state resource/action/exhaust tokens, Return Fame moving owned Fame cards to the top of the Fame deck including imported `suit:fame` icon metadata and player/public Exile sources, place-card-on-deck from both default hand and explicit discard sources, Give-card choices preserving both multiple card and multiple opponent choices before later text resumes, Look-card visibility/order choices including Nation Accession, ordinary Fame reordering above King of Kings, and special-bottom Fame handling, and data-driven keyword effects, plus `engine/src/tests/privateCardImport.test.ts`, `engine/src/tests/botTableCli.test.ts`, `engine/src/tests/nationRulesetValidation.test.ts`, and `engine/src/tests/privateCardEntryNavigation.test.tsx` coverage that private card import, Bot table import, nation ruleset validation, and entry UI accept the current runtime card-type vocabulary and all currently accepted human effect op shapes including resource movement, stealing, discard, nested conditional branches, counted Recall/Abandon Region, scoped Draw/Steal/Region target metadata, targeted Take-Unrest and Steal-resource attack metadata, `draw.upTo`, `draw_if_able.upTo`, `free_play_card`, free Develop, Return-Fame, Return-Exhaust-token, Treat As, Bot Exile, Bot bottom-deck movement, and the private-entry Bot table validator path while rejecting unsupported triggers, sources, suits, card-type filters, invalid numeric effect payloads, invalid effect destinations, missing required effect fields, empty Treat As replacement lists, empty nested effect branches, if-unable branches attached to Bot effects whose runtime path cannot consult them, empty Bot table rows/pay-then continuations, empty nation hook/custom effect payloads, unsupported human/Bot custom ruleset effect ops, malformed card/player identifier fields, malformed target-scope payloads, and malformed or ignored reactive Exhaust metadata before runtime, plus `engine/src/tests/turnLoop.test.ts` rollback and legality cases when unsupported or currently unresolvable effects surface through play, exhaust, acquire, Solstice, or nation hooks, including move-path rejection of optional-only cost-only text, optional-only zero-token resource returns, zero-count Look, Take-Unrest, and Gain-Fame text, zero-amount Gain Resource, Spend Resource, and Spend Action text, Return-Exhaust-token play rejection when no play-area Exhaust marker is returnable, partial finite-supply gains resolving when at least one requested token is available, fixed-recipient Give text whose named recipient is not legal, Take-Unrest text whose named recipients are not legal, non-market Gain-card text, Trade Routes-only text while the expansion is disabled, Trade effects receiving enabled expansion context through both play and Exhaust paths, Break-through fallback resource-gain interruption, and Break-through-only text with no card source or finite fallback Materials. Bot Exile Market eligibility is covered in `engine/src/tests/soloBotReview.test.ts` for both Market-resource tokens and generic card-state resource/action token markers.
- Treat As icon replacement is covered by `engine/src/tests/effectRunner.test.ts` cases for player-scoped replacement, original-icon removal during the turn, later same-text Acquire, Find, Exile, Gain/Take, Break through, Swap, and same-turn nation hook payload matching, and by `engine/src/tests/turnLoop.test.ts` for expiry before round-end Solstice.
- Nation ruleset validation is covered by `engine/src/tests/nationRulesetValidation.test.ts` cases for every current ruleset override family and hook condition shape, rulebook-to-engine resource-name normalization, private Bot cleanup Market token imports, `trade_routes_required` tag compatibility, compatibility checks defaulting missing imported ruleset tag/expansion/variant/short-game arrays to empty, standalone private-ruleset validator normalization/semantic checks, malformed human effect payload rejection for required source/suit/destination fields, effect identifier fields, nested empty effect branches, Bot custom cleanup required fields, numeric fields, fallback branches, filters, pay-then continuations, and reactive Exhaust metadata, and invalid resource rejection in top-level overrides, nested custom override effects, bot cleanup effects, and hook effect payloads. Runtime hook-condition coverage in `engine/src/tests/nationHookCore.test.ts` verifies missing expansion/variant option lists behave as disabled and missing payload-card tags behave as no tag match rather than crashing imported ruleset evaluation.
- Solstice/endgame timing is covered by `engine/src/tests/turnLoop.test.ts`, `engine/src/tests/fame.test.ts`, `engine/src/tests/scoring.test.ts`, `engine/src/tests/commonsDeckConstruction.test.ts`, `engine/src/tests/soloPracticeModes.test.ts`, and `engine/src/tests/soloBotReview.test.ts` cases for Solstice ordering including pending-choice and deck/state-mutating effects, Look-before-Draw and Place-on-Deck-before-Draw owner ordering, Return Fame order selection before pending choices, Return Exhaust token choice pause and resume of the remaining chosen Solstice cards, choice pause/resume, paused Solstice staying paused behind internal lifecycle continuations such as Market-Unrest hook continuation, before/after Solstice hooks, Collapse interruption after player-chosen Solstice order, normal-scoring final round handoff, Reactor-style End-of-Solstice removals after printed End-of-Solstice text including separated Accession removal, scoring option snapshots for short-game/practice timing, later option mutations in both normal-to-short-game and short-game-to-normal directions, and solo-vs-multiplayer finalization in both directions, Practice mode resolving the 12th turn's Solstice before final scoring while still skipping the normal extra final round, scoring finalization with minimal imported rulesets defaulting missing lifecycle arrays to empty, scoring finalization pauses including before- and after-scoring resource-gain reactive Exhaust windows and unresolved cleanup Market-resource/discard choices, Return Unrest from direct and garrisoned scored zones including garrisoned children attached to scored-zone hosts, Nation deck and Development area scoring exclusions including garrisoned children attached to unplayed hosts, structured card-resource VP formulas ignoring resources on Nation/Development hosts and their garrisoned cards even when those unscored zones are named, structured variable card-count VP formulas respecting History exclusions and scored History replacement zones, Collapse Unrest counting imported `suit:unrest` icon metadata while excluding ignored zones including garrisoned children attached to Nation, Development, and Exile hosts, Collapse tie-break scoring with minimal imported rulesets defaulting missing hook arrays to empty, per-card 10 VP caps, player resource-pool Progress scoring with rulebook resource aliases including state-gated ratio overrides, variable VP counting garrisoned cards attached to counted scoring zones, variable VP tag matching treating missing imported card tags as non-matches, variable VP counting player resource pools and explicit card-stored resources including rulebook resource aliases while only counting card-stored resources when explicit scored zones are named, Trade Route exclusion from generic human and Bot in-play counting including imported `suit:trade_route` icon metadata, solo Bot resource scoring with rulebook resource aliases, solo Bot VP valuation including negative VP, conditional best-branch scoring, Bot Power scoring exclusion including imported `suit:power` icon metadata, Bot Dynasty accession state-table flip during cleanup and data-driven skip exceptions, empty Bot/Dynasty top-card resolution gaining the rulebook fallback Materials while empty discard effects do not, short-game solo Bot setup removing the physical bottom Dynasty card before moving the current top Dynasty card to Bot discard and then applying extra Dynasty-discard short-game overrides, data-driven Bot cleanup Market resource replacement, paused solo Bot turns staying paused for later human pending-choice types such as Return-Exhaust-token and Look-order choices as well as internal lifecycle continuations such as Market-Unrest hook continuation, Bot-triggered Unrest-take continuations and Bot-given human Take-Unrest reactive Exhaust windows, Bot-given human resource reactive Exhaust windows including source-suited matches against Bot-triggered Trade Route sources, Bot row continuations waiting behind human pending choices, Bot custom cleanup continuations waiting behind human pending choices, Bot Trade Route commerce, Profit, and end-of-turn continuations waiting behind human pending choices, paid Bot Trade Route end-of-turn rows refunding costs and rolling back partial follow-up effects when the paid sequence cannot fully resolve, Bot table Trade Route triggers falling through when no route row resolves, and resuming after those choices resolve before Bot cleanup continues, Bot difficulty slot limits including die-blocked populated slots resolving one fewer Bot card while preserving the unresolved card through cleanup and placing the cleanup Market token, difficulty-specific Cultist Bot Unrest exceptions including imported `suit:unrest` icon metadata, Cultists most-tokened Market cleanup using structured Market slot resource markers, Bot setup Dynasty VP sorting with VP caps and randomized equal-VP ties, missing-tag imported cards being ignored by Bot setup tag scans, Bot Acquire tag filters, Bot table tag triggers, and Bot effect tag filters instead of crashing, random solo Bot setup skipping ruleset-excluded short-game nations, Bot market-token valuation with VP caps and structured Market slot markers, Bot Acquire tucked-Unrest collection from structured Market slot attachments, Bot Swap market-resource filters and transfers from both legacy Market-resource maps and structured Market slot markers applying before highest-value selection, Bot Trade Route top-Main VP-gated rewards, Bot Trade selection treating imported human `suit:trade_route` icon metadata as route eligibility, Bot Trade fallback preserving Goods when finite Progress supply makes conversion impossible, Bot Trade Route triggers requiring finite Goods and Progress supply before token placement/reward resolution, Bot Exile, Martian Bot Swap, Bot Abandon/Recall most-recent in-play selection, Supreme Ruler Return bonuses, and Main-deck Break-through reshuffling, Fame setup classifying imported `suit:fame` icon metadata into the Fame deck, Fame special-bottom-card timing including multi-count Fame gains and Fame draws crossing from ordinary Fame into the special bottom card without moving that card into hand/discard, free Develop not resuming reshuffle/draw or replacing the Fame scoring trigger, state-specific King of Kings reward suppression, Bot Fame rewards, and Collapse tie handling.
- Practice mode setup separation is covered by `engine/src/tests/soloPracticeModes.test.ts` for Practice creating its market-churn clock without initializing the full solo Bot state.
- Bot Return-from-discard imported-Unrest matching is covered by `engine/src/tests/soloBotReview.test.ts` for the most recent imported `suit:unrest` discard card returning to the shared pile and still awarding the Supreme Ruler return bonus.
- Bot destination-only table rows are covered by `engine/src/tests/soloBotReview.test.ts` for a revealed card moving to History and stopping fallback row resolution even when the matched row has no non-destination effect.
- Bot fallback timing is covered by `engine/src/tests/soloBotReview.test.ts` cases for if-unable Bot Trade Route fallback chains pausing behind human reactive Exhaust choices before resuming remaining fallback effects and later Commerce text.
- Bot table import expressiveness is covered by `engine/src/tests/botTableCli.test.ts` cases that accept every current Bot state-table effect op shape through both CLI/import validation and the private-entry validator, including filters, if-unable branches, pay-then continuations, `ifVp` branches, human-facing operations, state-table flips, merchant-state flips, Trade Route triggers, and log rows.
- Campaign metadata and progression are covered by `engine/src/tests/gameOptions.test.ts`, which normalizes Supreme Ruler campaign games to Supreme Ruler Bot difficulty, by `engine/src/tests/setupPipeline.test.ts`, which verifies the normalized campaign difficulty is stored on setup state, used by the Bot, applies the doubled starting-resource bonus after a campaign loss, and applies campaign starting-deck additions/removals before the opening hand, by `engine/src/tests/commonsSelection.test.ts`/`engine/src/tests/commonsSetup.test.ts`, which gate `supreme_ruler_campaign_extra` Commons cards to Supreme Ruler campaign setup, by `engine/src/tests/campaign.test.ts`, which covers standard campaign difficulty advancement, defeated-Bot restrictions, win deck choices, loss carryover resources, four-loss campaign loss, and Supreme Ruler set-aside/return bookkeeping, by `engine/src/tests/scoring.test.ts`, which verifies normal-scoring and solo-Collapse campaign outcome snapshots on `gameover`, and by `app/src/ui/layout/EndGameSummary.test.tsx`, which verifies the end-game reward choice surface, next campaign progress update helper, and public-safe campaign-sheet export payload.
- Variant setup is covered by `engine/src/tests/commonsDeckConstruction.test.ts`, `engine/src/tests/commonsReplacementPolicy.test.ts`, `engine/src/tests/commonsSetup.test.ts`, `engine/src/tests/setupPipeline.test.ts`, and `engine/src/tests/variants.test.ts` cases for Lowered Aggression delaying both attack/aggressive cards and explicitly `delayableInLoweredAggression` non-attack cards until after initial Market setup, Quick Setup using combined Commons construction while preserving Small decks and visible Tributary bottom cards, Commons replacement policy substituting direct `replacementForCardId` rows, applying `prefer_latest` freshness ordering, and handling nation-conflict/group replacements, Precious Cards suppressing only voluntary cleanup discard, and Short Game setup/progression exceptions.
- Trade Routes setup gating is covered by `engine/src/tests/commonsSelection.test.ts`, `engine/src/tests/commonsSetup.test.ts`, `engine/src/tests/setupPipeline.test.ts`, `engine/src/tests/expansionToggle.test.ts`, and `engine/src/tests/tradeRoutesModule.test.ts` cases for excluding Trade Routes-only cards while disabled, including Trade Routes Commons while enabled, suppressing mutually exclusive alternates, filtering/accepting expansion-required nations, rejecting imported rulesets tagged as Trade Routes-required when disabled, and enabling Trade Routes setup/runtime module behavior.
- State-model consistency is covered by the current typed zone model in `engine/src/game/state.ts`, setup/runtime-card inclusion in `engine/src/setup/setupPipeline.ts`, and `docs/rules-engine-notes.md` sections for stable vocabulary, state-shape priorities, and hidden-information policy.
- Market refill source-deck selection is covered by `engine/src/tests/turnLoop.test.ts` cases for matching Small-deck refill, main-deck fallback, and imported `suit:civilized` market suit icon metadata.
- Bot Exile acquisition Unrest classification is covered by `engine/src/tests/soloBotReview.test.ts` cases for printed Unrest, tag-only imported Unrest, and imported `suit:unrest` icon metadata avoiding the extra required Unrest card.
- High-risk areas reviewed in this pass include reshuffle/Development/State timing, draw-if-able safety, criteria-based Find searching all listed zones including explicit garrison sources, Treat As matching across keyword choices and same-turn hook payloads, market refill fallback, Acquire versus Break through side effects, Exile token ineligibility, payment substitution and resource operations, dynamic target scopes, per-target opponent choices, attack-protection targeting, garrison host/child movement, Power-area self-History movement, History replacement routing during setup and runtime movement, passive/Solstice timing, internal continuation ownership for turn, scoring, reshuffle, reshuffle draw/nation-hook handoff, Unrest, Market-Unrest, and Bot resume guards, Fame bottom-card availability, scoring exclusions, hidden-info selectors, human Trade Route active-player Commerce, solo Bot setup determinism, and Bot-only ruleset overrides being stripped from multiplayer/practice active rulesets.
